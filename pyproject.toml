[project]
name = "stego-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "V1NAY007", email = "shandilyavinay0@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "numpy>=1.24",
    "pillow>=9.0",
    "scipy>=1.10",
    "torch>=2.0",
]

[project.scripts]
stego-project = "stego_project:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
